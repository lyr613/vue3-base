import { ref, defineComponent } from "vue"
import style from "./s.module.less"

/** 组件 */
const TEMP = defineComponent({
    name: "TEMP",
    setup() {
        return () => <div></div>
    },
})

export default TEMP
